# Raspberry Pi 4B (VAE worker only). Minimal — no transformers, no accelerate.
# torch is installed separately by setup_pi.sh.
diffusers==0.30.3
safetensors>=0.4.3
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
zeroconf>=0.132.0
psutil>=5.9.8
pillow>=10.0.0
numpy<2
python-multipart>=0.0.9
